# click-stub

Tracer-bullet stub plugin used by Slice 1 to prove click-ai-devkit's install/uninstall
mechanism end to end (implementation-plan.md, Slice 1). This is not a real plugin — it is
replaced by `click-sdd`, `click-memory`, and `click-review` in later slices.
